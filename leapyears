        // Leap years :
        #include<stdio.h>
        int main(){
         for(int year=1900;year<=2023;year++)
         {
          if (year % 4==0 && year %100 !=0 || year%400==0)
          {
          printf("%d\n",year);
          }
          }
         
           return 0;
          }